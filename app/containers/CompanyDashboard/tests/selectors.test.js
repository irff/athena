// import { selectCompanyDashboardDomain } from '../selectors';
// import { fromJS } from 'immutable';
import expect from 'expect';

// const selector = selectCompanyDashboardDomain();

describe('selectCompanyDashboardDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
